#!/usr/bin/env python3
def no_c(my_string):
    c = [C, c]
    for i in my_string:
        if i != c:
            print(f{my_string})
        else:
            new_string = new_string + i
        return new_string
